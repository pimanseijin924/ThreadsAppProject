name: my_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: 
    sdk: flutter
  flutter_riverpod: ^2.0.0
  uuid: ^3.0.6
  http: ^0.13.5
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  device_info_plus: ^9.1.2
  intl: ^0.19.0
  firebase_core: ^2.15.0
  firebase_storage: ^11.2.5
  image_picker: ^1.0.7
  cloud_firestore: ^4.0.0
  #image_downloader: ^0.31.0 古いので代わりにimage_gallery_saverを使用
  #multi_image_picker: ^4.8.0
  file_picker: ^8.0.3
  #image_gallery_saver: ^2.0.3 これも古いので代わりにflutter_image_gallery_saverを使用
  flutter_image_gallery_saver: ^0.0.2
  dio: ^5.8.0
  go_router: ^13.2.0
  hive: ^2.2.3            # Lightweight key-value データベース :contentReference[oaicite:0]{index=0}
  hive_flutter: ^1.1.0    # Flutter での Hive 初期化やバインディングを簡単にする拡張 :contentReference[oaicite:1]{index=1}
  flutter_rating_bar: ^4.0.0 # 星評価ウィジェット
  #google_mobile_ads: ^3.0.0
  

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^1.1.0  # TypeAdapter の自動生成に必要 :contentReference[oaicite:2]{index=2}
  build_runner: ^2.3.3    # コード生成ツール
  change_app_package_name: ^1.5.0 # Android アプリのパッケージ名変更ツール
  flutter_launcher_icons: ^0.13.1 # アイコン生成ツール
  flutter_native_splash: ^2.2.12 # スプラッシュ画面生成ツール
  flutter_svg: ^1.1.6 # SVG画像を表示するためのパッケージ
  rename_app: ^1.6.2 # アプリ名変更ツール
  #flutter_local_notifications: ^15.0.0 # ローカル通知

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/app_icon_dev.png"
#``` :contentReference[oaicite:1]{index=1}  

flutter:
  generate: true
  uses-material-design: true
  
